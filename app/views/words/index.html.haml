%h1 Words
%p
  We have #{@words.count} words in Database!
%table.table
  %thead.thead-dark
    %th{scope: "col"} Id
    %th{scope: "col"} Content
    %th{scope: "col"} Language
    %th{scope: "col"} Actions
  %tbody
    - @words.each do |word|
      %tr
        %td= word.id
        %td= word.content
        %td= word.language&.name
        %td
          = link_to "Show", word, class: 'btn btn-primary'
          = link_to "Edit", edit_word_path(word), class: 'btn btn-primary'
